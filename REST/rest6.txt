Views.py: 


from django.shortcuts import render 
from django.core.mail import send_mail 
from django.conf import settings 
from django.http import HttpResponse 
def contact_email(request): 
    if request.method == 'POST': 
        # Get form data from POST request 
        name = request.POST.get('name') 
        email = request.POST.get('email') 
        subject = request.POST.get('subject') 
        message = request.POST.get('message') 
        full_message = f"Name: {name}\nEmail: {email}\n\nMessage:\n{message}" 
          try: 
            # Send the email (this example assumes you're using the default email backend) 
            send_mail( 
                subject,  
                full_message,  
                email,  # sender 
                [settings.CONTACT_EMAIL],  # recipient 
                fail_silently=False, 
            ) 
            # After sending, show a success message 
            return render(request, 'email_page.html', {'success': True}) 
         except Exception as e: 
            # If an error occurs during email sending, show an error message 
            return render(request, 'email_page.html', {'error': f"Error sending email: {str(e)}"}) 
     
    return render(request, 'email_page.html') 
 


urls.py:

 
from django.contrib import admin 
from django.urls import path 
from myapp7 import views 
urlpatterns = [ 
    path('admin/', admin.site.urls), 
    path('',views.contact_email,name='email_page'), 
] 



Settings.py 


EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend' 
EMAIL_HOST = 'smtp.gmail.com'  # For example, if using Gmail 
EMAIL_PORT = 587 
EMAIL_USE_TLS = True 
EMAIL_HOST_USER = 'strthangam2005@gmail.com' 
EMAIL_HOST_PASSWORD = 'wviezrarlcdjdbct'  # Make sure to handle credentials securely 
DEFAULT_FROM_EMAIL = 'dnainar121@gmail.com' 
CONTACT_EMAIL = 'dnainar121@gmail.com' 



Email_page.html: 


<!DOCTYPE html> 
<html lang="en"> 
<head> 
    <meta charset="UTF-8"> 
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
    <title>Contact Us</title> 
    <style> 
        /* General Styles */ 
        body { 
            font-family: Arial, sans-serif; 
            background-color: #f4f4f4; 
            margin: 0; 
            padding: 0; 
            box-sizing: border-box; 
        }h1 { 
            text-align: center; 
            color: #333; 
            margin-top: 50px; 
        } 
        form { 
            background-color: #fff; 
            padding: 20px; 
            margin: 20px auto; 
            max-width: 600px; 
            border-radius: 8px; 
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); 
        } 
        label { 
            font-weight: bold; 
            margin-bottom: 8px; 
            display: block; 
        } 
input, textarea { 
            width: 100%; 
            padding: 10px; 
            margin: 8px 0; 
            border: 1px solid #ccc; 
            border-radius: 4px; 
            font-size: 16px; 
        } 
  input:focus, textarea:focus { 
            border-color: #007BFF; 
            outline: none; 
        } 
  textarea { 
            resize: vertical; 
        } 
 button { 
            background-color: #007BFF; 
            color: white; 
            padding: 12px 20px; 
            border: none; 
            border-radius: 4px; 
            cursor: pointer; 
            font-size: 16px; 
            display: block; 
            width: 100%; 
            margin-top: 12px; 
        } 
button:hover { 
            background-color: #0056b3; 
        } 
 
        /* Success/Error Messages */ 
        .message { 
            padding: 15px; 
            margin: 20px auto; 
            max-width: 600px; 
            border-radius: 8px; 
            text-align: center; 
            font-weight: bold; 
        } 
 
        
 
 .success { 
            background-color: #28a745; 
            color: white; 
        } 
 
        .error { 
            background-color: #dc3545; 
            color: white; 
        } 
 
        /* Responsiveness */ 
        @media (max-width: 600px) { 
            form { 
                padding: 15px; 
                width: 90%; 
            } 
 
            button { 
                width: 100%; 
            } 
        } 
    </style> 
</head> 
<body> 
    <h1>Contact Us</h1> 
     
    {% if success %} 
        <div class="message success"> 
            Your message has been sent successfully! We will get back to you soon. 
        </div> 
    {% elif error %} 
        <div class="message error"> 
            There was an error sending your message. Please try again later. 
        </div> 
    {% endif %} 
     
    <form method="post"> 
        {% csrf_token %} 
        <label for="name">Name:</label><br> 
        <input type="text" id="name" name="name" required><br><br> 
       <label for="email">Email:</label><br> 
        <input type="email" id="email" name="email" required><br><br> 
         <label for="subject">Subject:</label><br> 
        <input type="text" id="subject" name="subject" required><br><br> 
        <label for="message">Message:</label><br> 
        <textarea id="message" name="message" rows="4" cols="50" required></textarea><br><br> 
       <button type="submit">Send Message</button> 
    </form> 
</body> 
</html> 